# Source store — the store data is read FROM (`query`, `export`).
SHOPIFY_SOURCE_SHOP=
SHOPIFY_SOURCE_TOKEN=

# Target store — the store data is written INTO (`import`).
SHOPIFY_TARGET_SHOP=
SHOPIFY_TARGET_TOKEN=
